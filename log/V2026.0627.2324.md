###### V2026.0627.2324 后续更新记录

##### 1. 后台版本页改进
- 版本信息卡片新增远程版本显示，与本地版本并排对比
- 远程版本状态自动判断：可更新（橙色）/ 已是最新（绿色）/ 未知（灰色）
- 移除"更新操作"面板，包括版本号输入框和手动更新按钮
- 远程版本"GitHub Releases"增加新窗口跳转链接

##### 2. 后台在线/离线更新功能
- 版本页面新增"在线更新"和"离线更新"按钮，仅在有新版本时显示
- 在线更新：从 GitHub Releases 下载最新 zipball，自动解压覆盖文件并更新版本号
- 离线更新：直接跳转 GitHub Releases 页面下载压缩包
- 在线更新排除 `.vscode`、`.git`、`.gitignore`、`data`等本地目录，避免覆盖配置
- 新增 `deleteDir`、`copyDir` 辅助函数，处理递归目录操作
- 修复在线更新 JSON 响应被 PHP warning 污染导致前端解析失败的问题
- 文件覆盖前先删除目标文件，解决权限不足导致 `copy()` 失败的问题
- 增加 `ZipArchive` 检查、`try-catch` 异常捕获、`ob_clean()` 输出缓冲清理
