####   更新日记

1. 增加后台自定义代码，用于控制前端页头页脚！
    - 新增 `data/data.php` 中的 `$customCode` 变量，用于存储自定义代码，支持 HTML、CSS、JS 等任意代码片段。
    - 在 `admin.php` 中，将 `$customCode` 变量作为参数传递给 `adminShowSettings()` 函数，并在页面中显示。
    - 在 `index.php` 中，将 `$customCode` 变量作为参数传递给 `showIndex()` 函数，并在页面中显示。